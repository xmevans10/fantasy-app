import CoreGraphics

/// Spacing scale (CDS `--gap/--pad`). One `comfortable` density for this consumer app.
enum Space {
    static let xs: CGFloat = 8
    static let sm: CGFloat = 12
    static let md: CGFloat = 16
    static let lg: CGFloat = 24
    static let xl: CGFloat = 32
}
